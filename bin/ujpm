#!/usr/bin/env node

const CLI = require('commander');
const META = require('../package.json');
const UJPM = require('../src/ujpm');

CLI.version(META.version, '-v, --version')

CLI.command('init')
  .description('Initialize the UJPM config')
  .option('-t, --target [value]', 'Set the target directory ', `ujpm`)
  .action((options) => {
    UJPM.init(options.target);
  });

CLI.command('install <package>')
  .alias('i')
  .description('Install a package')
  .action((package) => {
    UJPM.install(package);
  });

CLI.command('remove <package>')
  .alias('r')
  .description('Remove a package')
  .action((package) => {
    UJPM.remove(package);
  });

  CLI.command('update <package>')
  .alias('u')
  .description('Update a package')
  .action((package) => {
    console.log('update')
    console.log(package);
  });

CLI.parse(process.argv);
